# ?? ???? ???? - ???? ??????? ????????

## ?? ??????? ????????:

### ????? ???????:
```powershell
# ???? (??????)
.\quick-update.bat

# ?? ??????
.\update-version.ps1              # Patch: 1.0.0 ? 1.0.1
.\update-version.ps1 -Type minor  # Minor: 1.0.0 ? 1.1.0
.\update-version.ps1 -Type major  # Major: 1.0.0 ? 2.0.0
```

### ??????? ??????:
```bash
# Development
dotnet run

# Production
dotnet publish -c Release
```

---

## ?? ??????? ??????:

### Service Workers:
- `service-worker.js` ? Development
- `service-worker.published.js` ? Production ?

### ???????:
- `quick-update.bat` ? ???? ???????
- `update-version.ps1` ? ?????? ?????

### ???????:
- `SERVICE_WORKERS_GUIDE.md` ? ???? ?? ??? ?
- `QUICK_START.md` ? 3 ?????
- `README.md` ? ???? ????

---

## ?? ??????:

### ?? Console:
```javascript
localStorage.getItem('app_version')
await VersionManager.manualCheck()
```

### ?? DevTools:
- **Console** ? ????? ??????
- **Network** ? version.json
- **Application** ? Service Workers + Cache

---

## ?? ??????

```javascript
// ???? ?? ???
localStorage.clear();
caches.keys().then(n => Promise.all(n.map(k => caches.delete(k))));
navigator.serviceWorker.getRegistrations().then(r => r.forEach(x => x.unregister()));
location.reload(true);
```

---

## ?? ???? ??????:

1. **SERVICE_WORKERS_GUIDE.md** - ????? ??? ???????
2. **COMPLETION_SUMMARY.md** - ???? ????
3. **API_DOCUMENTATION.md** - JavaScript API

---

## ? ?????:

```
update-version.ps1 ? ???? ?? ??? ????????
                     (version.json + service-worker.js + service-worker.published.js)
```

**?? ??????? ?????? ??? ?????!**

---

**v1.0.1** • Made with ??
