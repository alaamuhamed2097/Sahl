namespace Common.Enumerations
{
    public enum AlertType
    {
        None,           // Default value when there's no specific alert
        NotVerified,    // Account is not verified
        ValidationError
    }
}
