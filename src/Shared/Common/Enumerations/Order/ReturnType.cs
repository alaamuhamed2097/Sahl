namespace Common.Enumerations.Order
{
    public enum ReturnType
    {
        FullReturn = 1,          // Complete money back
        PartialReturn = 2,        // Partial money back
        Replacement = 3,     // Send new item
        Repair = 4               // Fix item
    }
}