@page "/users/customer/edit/{id:guid}"

@using Common.Enumerations.User
@using Shared.DTOs.User.Customer
@inherits LocalizedComponentBase
@attribute [Authorize(Roles = nameof(UserRole.Admin))]

<PageTitle>@ECommerceResources.Customer</PageTitle>

<div class="container-fluid p-0">
    <div class="card shadow-sm border-0">
        <div class="card-header d-flex bg-primary justify-content-between align-items-center">
            <h4 class="modal-title text-white">
                <i class="bi bi-pencil-square fs-3 text-white"></i>
                ✏️ @ECommerceResources.EditCustomer
            </h4>
        </div>

        <EditForm Model="@Model" OnValidSubmit="Save">
            <DataAnnotationsValidator />

            <div class="card-body p-4">
                <!-- Personal Information -->
                <div class="row g-3">
                    <div class="col-md-6">
                        <div class="form-group">
                            <label>@ECommerceResources.FirstName <span class="text-danger">*</span></label>
                            <InputText type="text"
                                       placeholder="@($"{GeneralResources.Write} {ECommerceResources.FirstName}")"
                                       class="form-control"
                                       @bind-Value="Model.FirstName" />
                            <ValidationMessage For="@(() => Model.FirstName)" />
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-group">
                            <label>@ECommerceResources.LastName <span class="text-danger">*</span></label>
                            <InputText type="text"
                                       placeholder="@($"{GeneralResources.Write} {ECommerceResources.LastName}")"
                                       class="form-control"
                                       @bind-Value="Model.LastName" />
                            <ValidationMessage For="@(() => Model.LastName)" />
                        </div>
                    </div>
                </div>

                <!-- Email -->
                <div class="row g-3 mt-2">
                    <div class="col-md-12">
                        <div class="form-group">
                            <label>@ECommerceResources.Email</label>
                            <InputText type="email"
                                       placeholder="@($"{GeneralResources.Write} {ECommerceResources.Email}")"
                                       class="form-control"
                                       @bind-Value="Model.Email" />
                            <ValidationMessage For="@(() => Model.Email)" />
                        </div>
                    </div>
                </div>

                <!-- Password (Optional) -->
                <div class="row g-3 mt-2">
                    <div class="col-md-12">
                        <div class="form-group">
                            <label>@ECommerceResources.Password</label>
                            <InputText type="password"
                                       placeholder="@ECommerceResources.ChangePassword"
                                       class="form-control"
                                       @bind-Value="Model.NewPassword" />
                            <ValidationMessage For="@(() => Model.NewPassword)" />
                        </div>
                    </div>
                </div>
            </div>

            <div class="card-footer bg-light py-3 d-flex justify-content-end gap-2">
                <button type="button"
                        class="btn btn-responsive btn-secondary px-4"
                        @onclick="CloseModal"
                        disabled="@isSaving">
                    <i class="feather icon-x-circle me-2"></i>
                    @ActionsResources.Cancel
                </button>
                <button type="submit"
                        class="btn btn-primary btn-responsive text-white px-4"
                        disabled="@isSaving">
                    @if (isSaving)
                    {
                        <span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>
                        <span>@GeneralResources.Loading...</span>
                    }
                    else
                    {
                        <span>
                            <i class="feather icon-save me-2"></i>
                            @ActionsResources.Save
                        </span>
                    }
                </button>
            </div>
        </EditForm>
    </div>
</div>