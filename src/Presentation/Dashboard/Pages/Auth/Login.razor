@page "/login"
<PageTitle>@UserResources.LoginPageTitle</PageTitle>
@if (ResourceManager.CurrentLanguage == Language.Arabic)
{
    <style>
        .checkbox input[type=checkbox] + .cr:before {
            margin-right: 0 !important;
            margin-left: 10px !important;
        }
    </style>
}
<div class="auth-wrapper" dir="@(ResourceManager.CurrentLanguage == Language.Arabic ? "rtl" : "ltr")">
    <div class="auth-content container">
        <div class="card">
            <div class="row align-items-center">
                <div class="col-md-6 d-none d-md-block">
                    <img src="assets/images/auth/Login.svg" alt="Login" class="img-fluid" />
                </div>
                <div class="col-md-6">
                    <div class="card-body">
                        <div class="d-flex justify-content-center">
                            <img src="assets/images/logo-dark.svg" alt="logo" class="img-fluid mb-4" />
                        </div>
                        <h4 class="mb-3 f-w-400 text-center">
                            @UserResources.LoginPageTitle <span class="text-c-blue">@ECommerceResources.AdminTitle</span>
                        </h4>
                        <EditForm Model="_model" OnValidSubmit="HandleValidSubmit">
                            <DataAnnotationsValidator />
                            <div class="form-group mb-2">
                                <label class="form-label">@FormResources.EmailOrUsername</label>
                                <InputText type="text" class="form-control" placeholder="@UserResources.EmailOrUsernamePlaceholder" @bind-Value="_model.Identifier" />
                                <ValidationMessage For="@(() => _model.Identifier)" />
                            </div>
                            <div class="form-group mb-3">
                                <label class="form-label">@UserResources.PasswordLabel</label>
                                <InputText type="password" class="form-control" placeholder="@UserResources.PasswordPlaceholder" @bind-Value="_model.Password" />
                                <ValidationMessage For="@(() => _model.Password)" />
                            </div>
                            <div class="d-flex justify-content-end mb-3">
                                <button type="button" class="btn btn-link p-0 text-decoration-none" style="color:red;" @onclick="HandleForgetPassword">
                                    @UserResources.ForgetPasswordText
                                </button>
                            </div>
                            <button type="submit" class="btn btn-primary d-block mb-4 btf w-100 text-center" disabled="@_isSubmitting">
                                @if (_isSubmitting)
                                {
                                    <span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>
                                }
                                else
                                {
                                    @UserResources.LoginButton
                                }
                            </button>
                            @if (!string.IsNullOrEmpty(_errorMessage))
                            {
                                <div class="alert alert-danger mt-2">@_errorMessage</div>
                            }
                        </EditForm>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>