@page "/content/pages"
@page "/pages"

@using Common.Enumerations
@using Common.Enumerations.User
@using Microsoft.AspNetCore.Authorization
@using Resources
@using Shared.DTOs.Page
@using Shared.GeneralModels
@attribute [Authorize(Roles = nameof(UserRole.Admin))]
@inherits BaseListPage<PageDto>

<PageTitle>@GeneralResources.StaticPages</PageTitle>

<div class="container-fluid p-0">
    <div class="card shadow-sm border-0">
        <div class="card-header bg-primary text-white py-3 d-flex justify-content-between align-items-center">
            <h5 class="mb-0 fw-semibold text-white">📄 @GeneralResources.StaticPages</h5>
       <!-- Removed Add button since users can only edit existing pages -->
      </div>
      <div class="card-body">
            <div class="dt-responsive table-responsive">
                <!-- Filtration start -->
                <ul class="navbar-nav d-flex flex-row align-items-center gap-3 mb-2 px-0 w-100">
                    <li class="nav-item col-md-6 w-auto d-flex flex-row align-items-center gap-3">
                           <EditForm Model="searchModel" OnSubmit="@Search">
    <!-- Search Box -->
    <div class="input-group rounded search-box">
        <input type="text" @bind="searchModel.SearchTerm"
               id="searchInput"
               class="form-control text-center border-0 @(ResourceManager.CurrentLanguage == Language.Arabic ? "rounded-end" : "rounded-start")"
               placeholder="@ActionsResources.Search" />
        <button type="submit"
                class="btn btn-danger d-flex align-items-center justify-content-center border-0 @(ResourceManager.CurrentLanguage == Language.Arabic ? "rounded-start" : "rounded-end")">
            <i class="fas fa-search text-white m-0"></i>
        </button>
    </div>
</EditForm>

                                   <!-- Page Size -->
                                                                <div class="d-flex align-items-center gap-2">
                                            <select class="form-select rounded-3 shadow-sm border-light text-primary fw-bold text-center custom-dropdown"
                                                    style="width: 130px; height: 40px; padding-right: 30px;"
                                                    @onchange="OnPageSizeChanged">
                                                <option value="10"selected="@(searchModel.PageSize == 10)">10</option>
                                                <option value="15"selected="@(searchModel.PageSize == 15)">15</option>
                                                <option value="20"selected="@(searchModel.PageSize == 20)">20</option>
                                                <option value="30"selected="@(searchModel.PageSize == 30)">30</option>
                                            </select>
                                        </div>
                    </li>
                    <li class="nav-item ms-auto d-flex flex-row align-items-center gap-2">
                        <!-- Export Buttons -->
                        <div class="dropdown">
                            <button class="btn btn-info" type="button" id="exportDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="fas fa-file-export mx-1"></i>
                                <span class="fw-bold">@ActionsResources.Export</span>
                            </button>
                            <ul class="dropdown-menu" aria-labelledby="exportDropdown">
                                <li><a class="dropdown-item" href="javascript:void(0)" @onclick="ExportToExcel"><i class="far fa-file-excel mx-2"></i>@GeneralResources.Excel</a></li>
                                <li><a class="dropdown-item" href="javascript:void(0)" @onclick="ExportToPrint"><i class="fas fa-print mx-2"></i>@ActionsResources.Print</a></li>
                            </ul>
                        </div>
                    </li>
                </ul>
                
                <!-- Table -->
                <div class="table-responsive  border rounded-3 overflow-hidden">
                    <table class="table table-hover mb-0 d-none d-md-table">
                        <thead class="bg-light">
                            <tr>
                                <th class="text-center align-middle" style="width: 8%;">#</th>
                                <th class="text-center align-middle" style="width: 30%;">@FormResources.Title</th>
                                <th class="text-center align-middle" style="width: 35%;">@FormResources.ShortDescription</th>
                                <th class="text-center align-middle" style="width: 12%;">@FormResources.CreationDate</th>
                                <th class="text-center align-middle" style="width: 15%;">@FormResources.Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (items != null && items.Any())
                            {
                                int i = (currentPage - 1) * searchModel.PageSize;
                                @foreach (var item in items)
                                {
                                    <tr class="align-middle">
                                        <td class="text-center p-3">@(++i)</td>
                                        <td class="text-center align-middle">
                                            <div class="fw-bold text-truncate" style="max-width: 200px;" title="@item.Title">@item.Title</div>
                                        </td>
                                        <td class="text-center align-middle">
                                            <div class="text-truncate" style="max-width: 250px;" title="@(item.ShortDescription ?? item.Content)">
                                                @GetTruncatedContent(item.ShortDescription ?? item.Content, 60)
                                            </div>
                                        </td>
                                        <td class="text-center align-middle text-nowrap">@item.CreatedDateUtc.ToString("dd/MM/yyyy")</td>
                                        <td class="text-center align-middle">
                                            <div class="d-flex gap-1 justify-content-center">
                                                <button class="btn btn-info btn-sm" @onclick="() => Preview(item)" title="@ActionsResources.Preview">
                                                    <i class="fas fa-eye"></i>
                                                </button>
                                                <button class="btn btn-primary btn-sm" @onclick="() => EditPage(item)" title="@ActionsResources.Edit">
                                                    <i class="fas fa-edit"></i>
                                                </button>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            }
                            else
							{
			     <tr>
          <td colspan="7" class="p-0">
        <div class="d-flex flex-column justify-content-center align-items-center text-center" style="height: 600px;">
            <!-- أيقونة كبيرة -->
            <i class="fas fa-database text-muted" style="font-size: 60px;"></i>

            <!-- نص -->
            <p class="text-muted mt-3" style="font-size: 18px;">
                @NotifiAndAlertsResources.NoDataFound
            </p>
        </div>
    </td>
    </tr>
                            }
                        </tbody>
                    </table>

                    <!-- Mobile Responsive Cards -->
                    <div class="d-md-none">
                        @if (items != null && items.Any())
                        {
                            @foreach (var item in items)
                            {
                                <div class="card mb-3 border-0 shadow-sm">
                                    <div class="card-body">
                                        <div class="d-flex justify-content-between align-items-start mb-2">
                                            <h6 class="card-title mb-0 fw-bold">@item.TitleEn</h6>
                                        </div>
                                        <p class="card-text text-muted mb-2">@item.TitleAr</p>
                                        <p class="card-text"><small class="text-muted">Created: @item.CreatedDateUtc.ToString("dd-MM-yyyy")</small></p>
                                        <div class="d-flex gap-1">
                                            <button class="btn btn-info btn-sm" @onclick="() => Preview(item)">
                                                <i class="fas fa-eye"></i>
                                            </button>
                                            <button class="btn btn-primary btn-sm" @onclick="() => EditPage(item)">
                                                <i class="fas fa-edit"></i>
                                            </button>
                                            <button class="btn btn-secondary btn-sm" @onclick="() => ViewPage(item)">
                                                <i class="fas fa-external-link-alt"></i>
                                            </button>
                                            <!-- Removed Delete button since pages are fixed -->
                                        </div>
                                    </div>
                                </div>
                            }
                        }
                        else
                        {
                                      
        <div class="d-flex flex-column justify-content-center align-items-center text-center" style="height: 500px;">
            <!-- أيقونة كبيرة -->
            <i class="fas fa-database text-muted" style="font-size: 60px;"></i>

            <!-- نص -->
            <p class="text-muted mt-3" style="font-size: 18px;">
                @NotifiAndAlertsResources.NoDataFound
            </p>
        </div>
    
                        }
                    </div>
                </div>

                <!-- Pagination -->
                <div class="pagination-wrapper d-flex flex-column justify-content-end flex-grow-1">
    <div class="d-flex flex-wrap align-items-center justify-content-between gap-3 mt-4 pagination-container" dir="ltr">

        <!-- معلومات العرض -->
        <div class="text-muted small">
            @string.Format(ActionsResources.ShowingEntries, StartRecord, EndRecord, totalRecords)
        </div>

        <!-- الباجينيشن -->
        <nav aria-label="Page navigation">
            <ul class="pagination pagination-sm mb-0 shadow-sm rounded-3 overflow-hidden">

                <!-- أول صفحة -->
                <li class="page-item @(currentPage == 1 ? "disabled" : "")">
                    <a class="page-link" @onclick="() => GoToPage(1)" title="@ActionsResources.First">
                        <i class="fas fa-angle-double-left"></i>
                    </a>
                </li>

                <!-- السابقة -->
                <li class="page-item @(currentPage == 1 ? "disabled" : "")">
                    <a class="page-link" @onclick="() => GoToPage(currentPage - 1)">
                        <i class="fas fa-angle-left"></i>
                    </a>
                </li>

                <!-- الأرقام الديناميكية -->
                @{
                    const int maxVisiblePages = 5;
                    int startPage = Math.Max(1, currentPage - 2);
                    int endPage = Math.Min(totalPages, startPage + maxVisiblePages - 1);
                    if (endPage - startPage + 1 < maxVisiblePages)
                    {
                        startPage = Math.Max(1, endPage - maxVisiblePages + 1);
                    }
                }

                @if (startPage > 1)
                {
                    <li class="page-item disabled"><span class="page-link">...</span></li>
                }

                @for (int pag = startPage; pag <= endPage; pag++)
                {
                    int pageNumber = pag;
                    <li class="page-item @(pag == currentPage ? "active" : "")">
                        <a class="page-link fw-bold" @onclick="() => GoToPage(pageNumber)">@pag</a>
                    </li>
                }

                @if (endPage < totalPages)
                {
                    <li class="page-item disabled"><span class="page-link">...</span></li>
                }

                <!-- التالية -->
                <li class="page-item @(CanGoToNextPage ? "" : "disabled")">
                    <a class="page-link" @onclick="() => GoToPage(currentPage + 1)">
                        <i class="fas fa-angle-right"></i>
                    </a>
                </li>

                <!-- آخر صفحة -->
                <li class="page-item @(CanGoToNextPage ? "" : "disabled")">
                    <a class="page-link" @onclick="() => GoToPage(totalPages)" title="@ActionsResources.Last">
                        <i class="fas fa-angle-double-right"></i>
                    </a>
                </li>

            </ul>
        </nav>
    </div>
</div>

            </div>
        </div>
    </div>
</div>

<!-- Preview Modal -->
<div class="modal fade" id="previewModal" tabindex="-1" aria-labelledby="previewModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-xl">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="previewModalLabel">@ActionsResources.Preview</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="@ActionsResources.Close"></button>
            </div>
            <div class="modal-body" id="previewModalBody">
                <!-- Preview content will be loaded here -->
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">@ActionsResources.Close</button>
            </div>
        </div>
    </div>
</div>

<script>
    window.showPreviewModal = function (title, content) {
        document.getElementById('previewModalLabel').textContent = title;
        document.getElementById('previewModalBody').innerHTML = content;
        var modal = new bootstrap.Modal(document.getElementById('previewModal'));
        modal.show();
    };
</script>