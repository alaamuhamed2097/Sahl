@page "/reset-password"
@page "/authentication/reset-password"

<PageTitle>@UserResources.ResetPasswordTitle</PageTitle>
@if (ResourceManager.CurrentLanguage == Language.Arabic)
{
    <style>
        .checkbox input[type=checkbox] + .cr:before {
            margin-right: 0 !important;
            margin-left: 10px !important;
        }
    </style>
}
<div class="auth-wrapper" dir="@(ResourceManager.CurrentLanguage == Language.Arabic ? "rtl" : "ltr")">
    <div class="auth-content container">
        <div class="card">
            <div class="row align-items-center">
                <div class="col-md-6 d-none d-md-block">
                    <img src="assets/images/auth/Login.svg" alt="Reset Password" class="img-fluid" />
                </div>
                <div class="col-md-6">
                    <div class="card-body">
                        <div class="d-flex justify-content-center">
                            <img src="assets/images/logo-dark.svg" alt="logo" class="img-fluid mb-4" />
                        </div>
                        <h4 class="mb-3 f-w-400 text-center">
                            @UserResources.ResetPasswordTitle <span class="text-c-blue">@ECommerceResources.AdminTitle</span>
                        </h4>
                        <EditForm Model="_model" OnValidSubmit="HandleValidSubmit">
                            <DataAnnotationsValidator />
                            <div class="form-group mb-3">
                                <label class="form-label">@UserResources.VerificationCodeLabel</label>
                                <InputText type="text" class="form-control" placeholder="@UserResources.VerificationCodePlaceholder" @bind-Value="_model.VerificationCode" autocomplete="one-time-code" />
                                <ValidationMessage For="@(() => _model.VerificationCode)" />
                            </div>
                            <div class="form-group mb-3">
                                <label class="form-label">@UserResources.NewPasswordLabel</label>
                                <InputText type="password" class="form-control" placeholder="@UserResources.NewPasswordPlaceholder" @bind-Value="_model.NewPassword" autocomplete="new-password" />
                                <ValidationMessage For="@(() => _model.NewPassword)" />
                            </div>
                            <div class="form-group mb-3">
                                <label class="form-label">@UserResources.PasswordConfirmationLabel</label>
                                <InputText type="password" class="form-control" placeholder="@UserResources.PasswordConfirmationPlaceholder" @bind-Value="_model.PasswordConfirmation" autocomplete="new-password" />
                                <ValidationMessage For="@(() => _model.PasswordConfirmation)" />
                            </div>
                            <button type="submit" class="btn btn-primary d-block mb-4 btf w-100 text-center" disabled="@_isSubmitting">
                                @if (_isSubmitting)
                                {
                                    <span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>
                                }
                                else
                                {
                                    @UserResources.ResetPasswordButton
                                }
                            </button>
                            @if (!string.IsNullOrEmpty(_errorMessage))
                            {
                                <div class="alert alert-danger mt-2">@_errorMessage</div>
                            }
                            @if (!string.IsNullOrEmpty(_successMessage))
                            {
                                <div class="alert alert-success mt-2">@_successMessage</div>
                            }
                        </EditForm>
                        <div class="d-flex justify-content-end mt-2">
                            <a href="/login" class="btn btn-link p-0 text-decoration-none">@UserResources.BackToLogin</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
