@page "/"
@page "/home"
@page "/dashboard"

@attribute [Authorize(Roles = "Admin,Vendor")]
@using Common.Enumerations.User
@using Microsoft.AspNetCore.Components.Authorization
@using Microsoft.AspNetCore.Components.Web.Virtualization
@using Dashboard.Components
@using Dashboard.Components.Statistics
<PageTitle>@GeneralResources.Home</PageTitle>

<!-- [ breadcrumb ] start -->
<div class="page-header">
	<div class="page-block">
		<div class="row align-items-center">
			<div class="col-md-12">
				<div class="page-header-title">
					<h5>@GeneralResources.Home</h5>
				</div>
				<ul class="breadcrumb">
					<li class="breadcrumb-item">
						<a href="/">
							<i class="feather icon-home"></i>
						</a>
					</li>
					<li class="breadcrumb-item"><a href="#!">@GeneralResources.Analytics</a></li>
				</ul>
			</div>
		</div>
	</div>
</div>
<!-- [ breadcrumb ] end -->

<div class="container-fluid py-4 dashboard-page">
    <!-- Loading Skeleton -->
    @if (isInitialLoading)
    {
        <div class="loading-skeleton">
            <div class="row g-3">
                @for (int i = 0; i < 4; i++)
                {
                    <div class="col-lg-3 col-md-6">
                        <div class="card skeleton-card">
                            <div class="card-body">
                                <div class="skeleton-avatar"></div>
                                <div class="skeleton-text skeleton-title"></div>
                                <div class="skeleton-text skeleton-subtitle"></div>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    }
    else
    {
        <!-- [ Main Content ] start -->
        
        <!-- Vendor Section -->
  @*       <AuthorizeView Roles="@nameof(UserRole.Vendor)">
            <Authorized> *@
                <LazyComponent LoadingText="@GeneralResources.LoadingDots">
                    <VendorStatisticsWidget />
                </LazyComponent>
          @*   </Authorized>
        </AuthorizeView>
 *@
        <!-- Admin Section -->
        @* <AuthorizeView Roles="@nameof(UserRole.Admin)">
            <Authorized>
                @if (loadAdminWidget)
                {
                    <LazyComponent LoadingText="Loading Dashboard...">
                        <AdminStatisticsWidget />
                    </LazyComponent>
                }
                else
                {
                    <div class="text-center p-5">
                        <div class="card border-0 shadow-sm">
                            <div class="card-body p-5">
                                <i class="fas fa-tachometer-alt text-primary mb-3" style="font-size: 3rem;"></i>
                                <h4 class="mb-3">Admin Dashboard</h4>
                                <p class="text-muted mb-4">View comprehensive analytics and system overview</p>
                                <button class="btn btn-primary btn-lg" @onclick="LoadAdminWidget">
                                    <i class="fas fa-chart-line me-2"></i>
                                    Load Admin Dashboard
                                </button>
                            </div>
                        </div>
                    </div>
                }
            </Authorized>
        </AuthorizeView> *@
        
        <!-- [ Main Content ] end -->
    }
</div>

@* <link href="/css/home-lazy-loading.min.css" rel="stylesheet" />
<script src="/assets/js/pages/statistic.js"></script> *@

